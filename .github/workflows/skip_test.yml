name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "common_nb_preprocessors/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "common_nb_preprocessors/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
